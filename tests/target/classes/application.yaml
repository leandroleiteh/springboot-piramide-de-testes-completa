server:
  port: 8081

spring:
  application:
    name: tests

  sql:
    init:
      mode: always

  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/starwars?useSSL=false
    username: admin
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

